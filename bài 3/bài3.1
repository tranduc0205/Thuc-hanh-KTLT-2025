def sum (a,b):
    print("sum=" + str(a+b))
    # (a+b) cho ra kết quả là kiểu số nguyên:int
    #str(a+b) cho ra kết quả chuyển từ số nguyên sang chuỗi
    # dấu + để nối hay chuỗi lại với nhau
sum(4,5)
sum(3,7)
print("tran nguyen viet duc")
print("mssv")
