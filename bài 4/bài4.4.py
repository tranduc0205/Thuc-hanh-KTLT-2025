ds = input("danh sach:").split( )
# split: tách chuỗi thành các chuỗi nhỏ dựa trên khoảng trắng
print(ds)

for so in ds :
    # lặp từng chuỗi nhỏ trong ds
    print(so)   # in lần lượt các chuỗi nhỏ ra màn hình